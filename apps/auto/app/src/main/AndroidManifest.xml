<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:appCategory="audio"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.BeeboEntertainmentAuto">

        <!-- Car UX restrictions on Android Automotive OS. Not required, so the
             app still installs on phones. No distractionOptimized meta-data on
             the activity: the watch party is a parked-only video surface, and
             AAOS blocks non-optimized activities while driving. -->
        <uses-library
            android:name="android.car"
            android:required="false" />

        <!-- Tells Android Auto this app has something to offer. Without this
             meta-data the car never scans the service below. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Both actions are required: Media3 clients use the first, Android
             Auto and AAOS scan for the second. Omitting the legacy one is the
             single most common reason an app never appears in the car. -->
        <service
            android:name=".media.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <!-- Lets Assistant and Gemini hand the app a spoken search. -->
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
            </intent-filter>
        </service>

        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Browse artwork has to be a content:// URI; this serves posters
             out of the app's cache, fetching them from the server on demand. -->
        <provider
            android:name=".media.ArtworkProvider"
            android:authorities="com.beeboentertainment.auto.artwork"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
